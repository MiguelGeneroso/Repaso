import {sumaCaracteres} from './buclesFor';
import {esParImpar} from './condicionales';

let arr1 : string[]=["Casa", "Coche", "Ciudad", "Cesta"]
let arr2 : string[]=["Barco", "Baca", "Bicicleta", "Balon", "Bisiesto", "Brasil"]
let arr3 : string[]=["Venezuela", "Veneno", "Voltaje"]

esParImpar(sumaCaracteres(arr1));
esParImpar(sumaCaracteres(arr2));
esParImpar(sumaCaracteres(arr3));
